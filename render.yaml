services:
  - type: web
    name: clustro-api
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npm run build:api && npm run prisma:migrate:deploy
    startCommand: npm run start:api
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: "*"
